# 2026-03-15 작업일지

## 작업 내용
- `AdsManager.cs` 신규 추가 - Unity Ads (LevelPlay) 하단 배너 광고 연동
- `GameManager.cs` 수정 - AdsManager 관련 주석 추가
- `docs/TODO.md` 업데이트 - 에디터 설정 완료, 리소스 교체 완료, 광고 섹션 추가
- LevelPlay SDK 설치 (`Assets/LevelPlay/`, `Assets/MobileDependencyResolver/`)

## 구현 상세

### AdsManager
- 싱글톤 + DontDestroyOnLoad 패턴
- `IUnityAdsInitializationListener`, `IUnityAdsLoadListener` 구현
- Android/iOS Game ID 및 Ad Unit ID 분기 (`#if UNITY_IOS`)
- `_testMode` Inspector 토글 (기본값 true)
- 초기화 완료 시 자동으로 배너 로드 → `BOTTOM_CENTER` 표시
- `ShowBanner()` / `HideBanner()` public 메서드 제공
- Android Game ID: 6065782 / iOS Game ID: 6065783

### TODO 변경사항
- 에디터 설정 5개 항목 완료 체크
- 리소스 교체 4개 항목 완료 체크 (배경 타일만 추후)
- 사운드 최후순위로 변경
- 광고(Unity Ads) 섹션 신규 추가

## 에디터 설정 필요 항목
- MainScene에 빈 GameObject 생성 → AdsManager 컴포넌트 추가
- 출시 전 `_testMode = false` 변경
- Android/iOS 실기기 빌드 테스트

## 다음 작업 후보
- 실기기 빌드 테스트 (배너 위치/크기 확인)
- iOS Ad Unit ID 별도 확인 후 분리
- 플레이테스트 & 밸런싱
- 메인 메뉴 배경 구성
