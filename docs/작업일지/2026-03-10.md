# 2026-03-10 작업일지

## 작업 내용
- SoundManager 시스템 추가 (런타임 자동 로드 방식)
- 플레이어 발사 효과음 연결 (PlayerFire)

## 구현 상세

### SoundManager (Assets/Scripts/Logic/System/SoundManager.cs)
- 싱글톤 패턴
- `Start()`에서 `Resources.LoadAll<AudioClip>("Sounds")`로 전체 클립 자동 로드
- `Dictionary<string, AudioClip>`에 clip.name 기준 캐싱
- `Play(string soundName)` 메서드: 이름으로 클립 검색 후 `PlayOneShot` 재생
- 클립 없을 시 `Debug.LogWarning`만 출력 (stub 역할 겸용)
- Inspector 연결 없이 파일만 넣으면 자동 인식

### Character.cs 수정
- `TryShoot()`에서 `SoundManager.Instance?.Play("PlayerFire")` 호출 추가

## 사운드 파일 위치
- `Assets/Resources/Sounds/` 폴더에 오디오 파일 배치 필요
- 현재 추가된 사운드: `PlayerFire.ogg`
- 나중에 파일만 추가하면 코드 수정 없이 `Play("파일명")` 으로 바로 사용 가능

## 에디터 설정 필요 항목
- SoundManager 오브젝트가 씬에 추가되어 있는지 확인 (GameScene에 자동 추가됨)

## 다음 작업 후보
- 추가 효과음 확보 후 연결 (적 피격, 대시, 레벨업, 보스 등장 등)
- BGM 추가 (SFX/BGM AudioSource 분리 필요)
- 보스 등장 경고 연출
